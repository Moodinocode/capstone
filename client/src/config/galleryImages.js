/**
 * Event photo gallery configuration.
 *
 * Drop your images into client/public/images/ then add their filenames here.
 * The alt text is used for accessibility — keep it descriptive.
 *
 * Example:
 *   { src: '/images/opening-ceremony.jpg', alt: 'Opening ceremony', span: 'wide' }
 *
 * span options:
 *   'normal' — standard square tile
 *   'wide'   — 2 columns wide (good for landscape/group shots)
 *   'tall'   — 2 rows tall (good for portrait shots)
 */

const galleryImages = [
  // ── Add your filenames below ──────────────────────────────────────────────
  // { src: '/images/your-photo.jpg', alt: 'Description', span: 'wide' },
  // { src: '/images/your-photo.jpg', alt: 'Description', span: 'normal' },
  // ─────────────────────────────────────────────────────────────────────────
];

export default galleryImages;
